'use strict';

let express = require('express');
let app = require('../app');
let request = require('supertest');
let assert = require('chai').assert;

describe('Init Test', function() {
  it('Index test ran', function(done) {
    done();
  });
});
